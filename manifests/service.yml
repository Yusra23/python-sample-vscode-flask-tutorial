apiVersion: v1
kind: Service
metadata:
    name: yusrapythonsamplevscodeflasktutorial
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: yusrapythonsamplevscodeflasktutorial